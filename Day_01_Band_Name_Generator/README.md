# Day 01 – Band Name Generator

## Project

A simple program that generates a band name based on user input.

## 📝 Task Description

In this project, the program:

* Displays a greeting message
* Asks the user for the city they grew up in
* Asks the user for the name of a pet
* Combines the city name and pet name to generate a band name

String concatenation and newline characters (`\n`) are used to format input and output properly.

## Concepts Practiced

* `print()` function
* `input()` function
* Variables
* String concatenation
* New lines using `\n`
